<%= content_tag :div, 'Dashboard>', class: 'section-title font-weight-bold text-uppercase bg-white w-100, p-3' %>
<div class="d-flex justify-content-between align-items-center">
  <p class='mb-0 price p-4 shadow bg-white w-100'>Total Amount: <span class='float-right'> <%= number_to_currency(current_user.total_grouped_expenses) || 0%> </span></p>
</div>
<div class='py-3 mt-3 mr-5'>
  <div class='bg-light mx-4'>
    <div class="row flex-column flex-md-row flex-wrap">
      <%= render_expense_collection(@expenses, 'expense') %>
    </div>
      <div class="custom-btn p-4 ">
      <%= link_to 'Add New', new_expense_path, class: 'btn btn-lg custom-btn border border-white w-100' %>
    </div>
  </div>
</div>
