<p class='mb-0 price p-4 shadow bg-white w-100'>Total Amount: <span class='float-right'> <%= number_to_currency(@team.total_expenses) || 0 %> </span></p>
<div class="mx-3">
  <%= render partial: 'team', locals: { team: @team } %>
</div>

<div class="d-flex flex-sm-column flex-md-row flex-wrap">
  <%= render_expense_collection(@team.expenses, 'expenses/expense')%>
</div>